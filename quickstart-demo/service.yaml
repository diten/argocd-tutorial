apiVersion: v1
kind: Service
metadata:
  name: nginx
  labels:
    run: nginx
spec:
  type: LoadBalancer
  ports:
  - port: 8090
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    run: nginx

